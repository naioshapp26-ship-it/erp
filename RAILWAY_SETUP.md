# دليل إعداد Railway ✈️

## ✅ التحديثات المنفذة

### 1. إصلاح الخادم للعمل على Railway
- **المشكلة**: الخادم كان يستمع على `localhost` فقط
- **الحل**: تم تغييره ليستمع على `0.0.0.0` (جميع واجهات الشبكة)
- **الكود**: 
  ```javascript
  const HOST = process.env.HOST || '0.0.0.0';
  app.listen(PORT, HOST, () => { ... });
  ```

### 2. إضافة Procfile
- **الملف**: `Procfile`
- **المحتوى**: `web: npm start`
- **الهدف**: إخبار Railway كيفية تشغيل التطبيق

---

## 🔧 إعدادات Railway المطلوبة

### المتغيرات البيئية (Environment Variables)
تأكد من إضافة المتغيرات التالية في لوحة Railway:

```env
DATABASE_URL=postgresql://postgres:PddzJpAQYezqknsntSzmCUlQYuYJldcT@crossover.proxy.rlwy.net:44255/railway
NODE_ENV=production
PORT=3000
```

**ملاحظة مهمة**: Railway يُعيّن `PORT` تلقائياً، لذا لا داعي لتحديده عادةً.

---

## 🚀 خطوات النشر على Railway

### 1. ربط المستودع
```bash
# في لوحة Railway:
# 1. New Project
# 2. Deploy from GitHub repo
# 3. اختر المستودع: KarimWajihKMW/-------------------1767723379340-y21rmmxm-6lgb7m
```

### 2. ضبط الإعدادات
```
Root Directory: /
Start Command: npm start (يتم قراءته من Procfile)
```

### 3. إضافة قاعدة البيانات
إذا لم تكن مرتبطة بالفعل:
```
# في Railway:
# 1. أضف متغير DATABASE_URL
# 2. تأكد من أن قاعدة البيانات PostgreSQL متاحة
```

---

## 🔍 التحقق من العمل

### 1. فحص Logs في Railway
بعد النشر، راجع السجلات (Logs) في لوحة Railway:

**المتوقع رؤيته**:
```
🚀 نظام نايوش يعمل على 0.0.0.0:3000
📊 API متاح على: http://localhost:3000/api
🌍 Environment: production
```

**إذا رأيت خطأ**:
- تحقق من أن جميع التبعيات (dependencies) مثبتة
- تأكد من متغير DATABASE_URL صحيح
- راجع سجل الأخطاء الكامل

### 2. اختبار API
بعد النشر، اختبر الـ API:
```bash
# استبدل YOUR_APP_URL برابط التطبيق على Railway
curl https://YOUR_APP_URL/api/health
```

**النتيجة المتوقعة**:
```json
{
  "status": "OK",
  "database": "Connected",
  "time": "2026-01-07T16:30:00.000Z"
}
```

---

## ⚠️ المشاكل الشائعة والحلول

### المشكلة 1: الخادم يتوقف بعد بضع ثوانٍ
**الأعراض**: 
```
Starting Container
npm error signal SIGTERM
Stopping Container
```

**الحلول**:
1. ✅ **تم الإصلاح**: الخادم الآن يستمع على `0.0.0.0`
2. تأكد من عدم وجود أخطاء في الكود
3. تحقق من أن `package.json` يحتوي على جميع التبعيات

### المشكلة 2: القائمة فارغة في الواجهة
**الأعراض**: القائمة الجانبية تعرض "جاري التحميل..." ولا تظهر العناصر

**الحلول**:
1. ✅ **تم الإصلاح**: إضافة بيانات احتياطية (fallback data)
2. تحقق من أن API يستجيب بشكل صحيح
3. افتح Developer Tools في المتصفح (F12) وتحقق من:
   - Console للأخطاء
   - Network لرؤية طلبات API

### المشكلة 3: قائمة تبديل المستأجر فارغة
**الأعراض**: عند الضغط على "تبديل المستأجر" لا تظهر المستخدمين

**الحلول**:
1. ✅ **تم الإصلاح**: دالة `toggleRoleMenu` الآن تملأ القائمة ديناميكياً
2. تأكد من أن `/api/users` يرجع بيانات
3. افتح Console وتحقق من رسائل الأخطاء

---

## 📊 التحقق من الواجهة الأمامية

### 1. افتح المتصفح Developer Tools (F12)
### 2. راجع Console للرسائل التالية:

**عند تحميل الصفحة**:
```
🔄 بدء التهيئة...
📊 تم تحميل البيانات: { entities: X, users: Y }
👤 المستخدم الحالي: {...}
🔄 رسم القائمة الجانبية...
✅ اكتملت التهيئة
```

**إذا رأيت**:
```
❌ خطأ في تحميل البيانات: ...
⚠️ استخدام البيانات الاحتياطية...
```
معناه أن API لا يستجيب، لكن التطبيق سيعمل بوضع محلي (offline).

---

## 🌐 رابط التطبيق المنشور

بعد النشر على Railway، ستحصل على رابط مثل:
```
https://your-app-name.up.railway.app
```

### اختبار سريع:
1. افتح الرابط في المتصفح
2. انتظر حتى يتم تحميل الصفحة
3. افتح Developer Tools (F12)
4. تحقق من Console
5. جرب الضغط على "تبديل المستأجر"

---

## 📝 ملفات المشروع الحالية

```
✅ server.js - الخادم (محدث ليستمع على 0.0.0.0)
✅ script.js - الواجهة الأمامية (محدثة مع بيانات احتياطية)
✅ package.json - التبعيات
✅ Procfile - تكوين Railway
✅ .env - المتغيرات البيئية (لا تُنشر على git)
```

---

## 🎯 الحالة الحالية

- ✅ الخادم يعمل على `0.0.0.0`
- ✅ Procfile موجود
- ✅ البيانات الاحتياطية متوفرة
- ✅ قائمة تبديل المستأجر تعمل
- ✅ معالجة الأخطاء محسّنة
- ✅ الكود منشور على GitHub

**الخطوة التالية**: 
انتظر حتى يُعاد نشر التطبيق على Railway تلقائياً (عادةً يستغرق 2-3 دقائق بعد push إلى GitHub)، ثم جرب فتح الرابط مرة أخرى.

---

## 🆘 إذا استمرت المشكلة

1. **تحقق من Logs في Railway**:
   - افتح لوحة Railway
   - اذهب إلى "Deployments"
   - افتح آخر deployment
   - راجع "Build Logs" و "Deploy Logs"

2. **أرسل السجلات (Logs)**:
   - انسخ السجلات الكاملة من Railway
   - شاركها معي لتحليل المشكلة

3. **تحقق من المتغيرات البيئية**:
   - تأكد من أن DATABASE_URL صحيح
   - تأكد من عدم وجود مسافات زائدة

---

**آخر تحديث**: 7 يناير 2026
**الحالة**: ✅ جاهز للنشر
