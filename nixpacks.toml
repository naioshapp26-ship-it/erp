[variables]
NIXPACKS_PATH = "/app/.nixpacks"

[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm install --production"]

[start]
cmd = "node server.js"
