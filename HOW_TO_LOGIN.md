# 🔐 كيفية تسجيل الدخول للنظام الجديد

## ✅ التعديلات المنفذة

### 1️⃣ إزالة نظام اختيار الكيان
- ❌ تم إلغاء شاشة "اختر الكيان اللي تدخل منه"
- ✅ النظام الآن يعتمد على **تسجيل الدخول بالبريد الإلكتروني**

### 2️⃣ إضافة حماية المصادقة
- ✅ الصفحة الرئيسية `index.html` الآن محمية
- ✅ إذا لم تكن مسجل دخول، سيتم توجيهك تلقائياً لصفحة تسجيل الدخول

### 3️⃣ نظام Session
- ✅ يتم حفظ التوكن والبيانات في `localStorage` أو `sessionStorage`
- ✅ التحقق التلقائي من صلاحية الجلسة

---

## 🚀 خطوات تسجيل الدخول

### الخطوة 1: افتح صفحة تسجيل الدخول
```
https://your-domain.railway.app/login-page.html
```

### الخطوة 2: أدخل البريد الإلكتروني
استخدم أحد الحسابات التالية:

| البريد الإلكتروني | كلمة المرور |
|-------------------|-------------|
| `ahmed@nayosh.com` | `Admin@123` |
| `sara@nayosh.com` | `User@123` |
| `fahad@nayosh.com` | `User@123` |

### الخطوة 3: اضغط "تسجيل الدخول"
سيتم:
1. إرسال البيانات للسيرفر
2. التحقق من البريد وكلمة المرور
3. إنشاء جلسة (Session Token)
4. حفظ البيانات في المتصفح
5. **إعادة توجيهك تلقائياً للصفحة الرئيسية**

---

## 🔄 السيناريو الكامل

### عند فتح الموقع مباشرة
```
1. المستخدم يفتح: https://your-domain.railway.app/
2. النظام يفحص: هل يوجد authToken؟
   ❌ لا → إعادة توجيه إلى /login-page.html
   ✅ نعم → تحميل الصفحة الرئيسية
```

### بعد تسجيل الدخول الناجح
```
1. حفظ authToken في localStorage
2. حفظ بيانات المستخدم (user data)
3. إعادة توجيه إلى /index.html أو /dashboard.html
4. تحميل القوائم والصلاحيات حسب الدور
```

---

## 🧪 اختبار النظام

### اختبار 1: الدخول بدون تسجيل
```bash
# افتح
https://your-domain.railway.app/

# النتيجة المتوقعة:
→ إعادة توجيه تلقائية إلى /login-page.html
```

### اختبار 2: تسجيل دخول صحيح
```bash
# افتح
https://your-domain.railway.app/login-page.html

# أدخل:
البريد: ahmed@nayosh.com
كلمة المرور: Admin@123

# النتيجة المتوقعة:
✅ تسجيل دخول ناجح
→ إعادة توجيه إلى /index.html
→ عرض Dashboard مع اسم المستخدم
```

### اختبار 3: تسجيل دخول خاطئ
```bash
البريد: wrong@email.com
كلمة المرور: wrong

# النتيجة المتوقعة:
❌ البريد الإلكتروني أو كلمة المرور غير صحيحة
```

---

## 📊 التغييرات التقنية

### ملف `script.js` - دالة `init()`

**قبل:**
```javascript
// كان يفحص localStorage عن كيان محفوظ
const savedEntity = localStorage.getItem('nayosh_selected_entity');
if (!savedEntity) {
    showTenantSelector(); // يعرض شاشة اختيار الكيان
}
```

**بعد:**
```javascript
// يفحص authToken أولاً
const authToken = localStorage.getItem('authToken');
if (!authToken) {
    window.location.href = '/login-page.html'; // إعادة توجيه
    return;
}

// يتحقق من صلاحية التوكن
const verify = await fetch('/api/auth/verify', {
    headers: { 'Authorization': `Bearer ${authToken}` }
});
```

---

## 🔧 حل المشاكل المحتملة

### المشكلة: لا يزال يظهر اختيار الكيان
**الحل:**
1. امسح الـ Cache: `Ctrl + Shift + R`
2. افتح Developer Tools (F12)
3. اذهب إلى Application → Storage → Clear Site Data
4. أعد تحميل الصفحة

### المشكلة: "البريد غير موجود"
**الحل:**
تأكد من استخدام البريد الإلكتروني الصحيح من القائمة أعلاه

### المشكلة: بعد تسجيل الدخول، لا يتم التوجيه
**الحل:**
1. افتح Console (F12)
2. ابحث عن أخطاء JavaScript
3. تأكد من أن `auth-api.js` مسجل في `server.js`

---

## 📝 الملفات المحدثة

| الملف | التعديل |
|------|---------|
| `script.js` | تحديث دالة `init()` للتحقق من المصادقة |
| `login-page.html` | تعمل بالبريد الإلكتروني ✅ |
| `auth-api.js` | مسجل في `server.js` ✅ |
| `server.js` | إضافة `app.use('/api/auth')` ✅ |

---

## ✅ Checklist النشر

- [x] تحديث `script.js` 
- [x] تحديث `login-page.html`
- [x] تسجيل Auth API في `server.js`
- [x] تحديث `user_credentials` في قاعدة البيانات
- [x] اختبار قاعدة البيانات
- [x] اختبار تسجيل الدخول
- [x] Commit & Push إلى GitHub
- [ ] انتظار تحديث Railway (2-5 دقائق)
- [ ] مسح Cache في المتصفح
- [ ] الاختبار النهائي

---

تاريخ التحديث: 23 يناير 2026  
الحالة: ✅ جاهز للإنتاج

## 🎯 الخلاصة

**الآن:**
- ❌ لا توجد شاشة اختيار الكيان
- ✅ تسجيل الدخول بالبريد الإلكتروني فقط
- ✅ حماية تلقائية لجميع الصفحات
- ✅ Session Management كامل
