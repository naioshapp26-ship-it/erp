# نظام نايوش ERP - نظام تخطيط موارد المؤسسات متعدد المستأجرين

## ✨ حالة المشروع: **مكتمل** ✅

### مراحل التطوير:
- ✅ **المرحلة 1**: هيكل المنصة Multi-Tenant + هرم الكيانات (100%)
- ✅ **المرحلة 2**: ربط كل شيء بالكيان بتاعه (100%)
- ✅ **المرحلة 3**: عزل البيانات (Data Isolation) - **مكتملة الآن!** (100%)

## 🌟 الميزات

- ✅ نظام متعدد المستأجرين (Multi-Tenant SaaS)
- ✅ قاعدة بيانات PostgreSQL متصلة بالكامل
- ✅ Backend API مع Node.js و Express
- ✅ Frontend ديناميكي مع Tailwind CSS
- ✅ إدارة الكيانات والمستخدمين
- ✅ نظام الفواتير والمدفوعات
- ✅ نظام الإعلانات متعدد المستويات
- ✅ دفتر الأستاذ المحاسبي
- ✅ **عزل البيانات متقدم (Data Isolation)** - كل كيان يرى فقط بياناته
- ✅ نظام الموافقات (Approval Workflows)

## 📦 التثبيت والتشغيل

### 1. تثبيت المتطلبات

```bash
npm install
```

### 2. تهيئة قاعدة البيانات

```bash
node init-db.js
```

### 3. اختبار قاعدة البيانات

```bash
node test-db.js
```

### 4. تشغيل السيرفر

```bash
npm start
```

أو للتطوير مع إعادة التشغيل التلقائي:

```bash
npm run dev
```

### 5. فتح التطبيق

افتح المتصفح على: `http://localhost:3000`

## 🔐 نظام عزل البيانات

لمعلومات مفصلة عن نظام عزل البيانات وكيفية استخدامه:
👉 [اقرأ دليل عزل البيانات](DATA_ISOLATION_GUIDE.md)

### الاستخدام السريع:

```bash
# HQ يرى كل البيانات
curl -H "x-entity-type: HQ" -H "x-entity-id: HQ001" \
  http://localhost:3000/api/employees

# الفرع يرى بيانات فرعه فقط
curl -H "x-entity-type: BRANCH" -H "x-entity-id: BR015" \
  http://localhost:3000/api/employees
```

## 🔧 بنية المشروع

```
.
├── index.html          # الواجهة الأمامية
├── script.js           # منطق التطبيق + API Integration
├── style.css           # التنسيقات المخصصة
├── server.js           # Backend API Server
├── db.js               # إعدادات قاعدة البيانات
├── init-db.sql         # SQL Schema
├── init-db.js          # سكريبت تهيئة قاعدة البيانات
├── test-db.js          # اختبارات قاعدة البيانات
├── test-api.js         # اختبارات API
├── package.json        # متطلبات المشروع
└── .env                # إعدادات البيئة (غير مضمن في Git)
```

## 🗄️ قاعدة البيانات

### الجداول المتوفرة:

- **entities** - الكيانات والمستأجرين
- **users** - المستخدمين
- **invoices** - الفواتير
- **transactions** - المعاملات المالية
- **ledger** - دفتر الأستاذ المحاسبي
- **ads** - الإعلانات

### معلومات الاتصال:

- Host: `crossover.proxy.rlwy.net:44255`
- Database: `railway`
- Username: `postgres`
- Password: محفوظ في `.env`

## 🧪 الاختبارات

### اختبار قاعدة البيانات:
```bash
npm test
```

### اختبار API:
```bash
node test-api.js
```

## 📡 API Endpoints

- `GET /api/health` - التحقق من حالة النظام
- `GET /api/entities` - جميع الكيانات
- `GET /api/users` - جميع المستخدمين
- `GET /api/invoices` - جميع الفواتير
- `GET /api/transactions` - جميع المعاملات
- `GET /api/ledger` - دفتر الأستاذ
- `GET /api/ads` - جميع الإعلانات
- `POST /api/ads` - إنشاء إعلان جديد
- `PUT /api/ads/:id` - تحديث إعلان
- `GET /api/stats` - إحصائيات النظام

## 👥 المستخدمون الافتراضيون

1. م. أحمد العلي - المكتب الرئيسي (HQ)
2. سارة محمد - فرع العليا مول (Branch)
3. د. خالد الزهراني - حاضنة السلامة (Incubator)
4. فريق التقنية - نايوش كلاود (Platform)

## 🚀 التطوير

تم بناء المشروع باستخدام:

- **Frontend**: HTML5, TailwindCSS, Vanilla JavaScript
- **Backend**: Node.js, Express.js
- **Database**: PostgreSQL
- **Hosting**: Railway

## 📝 ملاحظات

- جميع البيانات محفوظة في قاعدة البيانات PostgreSQL
- التطبيق يدعم العزل الكامل بين المستأجرين (Tenant Isolation)
- النظام يدعم 5 مستويات للإعلانات من المحلي إلى العالمي

---

تم التطوير بواسطة فريق نايوش © 2024
