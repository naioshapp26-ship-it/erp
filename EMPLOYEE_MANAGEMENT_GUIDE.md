# دليل إدارة الموظفين - نظام نايوش

## 🎯 نظرة عامة
تم تطوير نظام إدارة الموظفين كجزء من البنية الهرمية متعددة المستأجرين، يسمح بإدارة شاملة للموظفين عبر جميع الكيانات (HQ، فروع، حاضنات، منصات، مكاتب).

## 🏗️ البنية التقنية

### قاعدة البيانات
```sql
-- جدول الموظفين الرئيسي
employees (
    id SERIAL PRIMARY KEY,
    employee_number VARCHAR(50) UNIQUE NOT NULL,
    full_name VARCHAR(255) NOT NULL,
    email VARCHAR(255),
    phone VARCHAR(20),
    national_id VARCHAR(20),
    position VARCHAR(100) NOT NULL,
    department VARCHAR(100) NOT NULL,
    hire_date DATE,
    salary DECIMAL(10,2),
    employment_type employment_type_enum NOT NULL,
    address TEXT,
    emergency_contact VARCHAR(255),
    emergency_phone VARCHAR(20),
    
    -- ربط بالكيانات الهرمية
    assigned_hq BOOLEAN DEFAULT FALSE,
    assigned_branch_id INTEGER REFERENCES branches(id),
    assigned_incubator_id INTEGER REFERENCES incubators(id), 
    assigned_platform_id INTEGER REFERENCES platforms(id),
    assigned_office_id INTEGER REFERENCES offices(id),
    
    -- معلومات النظام
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### أنواع التوظيف
- `FULL_TIME` - دوام كامل
- `PART_TIME` - دوام جزئي  
- `CONTRACT` - عقد
- `INTERN` - متدرب

## 🚀 الميزات المتاحة

### 1. عرض الموظفين
- **إحصائيات شاملة**: عدد الموظفين حسب نوع الكيان (5 كروت إحصائية)
- **جدول تفاعلي**: عرض جميع بيانات الموظفين مع خيارات التفاعل
- **البحث والتصفية**: بحث بالاسم وتصفية حسب نوع الكيان
- **أزرار العمل**: عرض، تعديل، حذف لكل موظف

### 2. إضافة موظف جديد
- **نموذج شامل**: يشمل جميع البيانات الأساسية والوظيفية
- **ربط بالكيانات**: إمكانية تعيين الموظف لأي كيان هرمي
- **التحقق من البيانات**: validation للحقول المطلوبة

### 3. إدارة العلاقات
- **ربط هرمي**: كل موظف يرتبط بكيان واحد فقط
- **مرونة الانتقال**: يمكن نقل الموظف بين الكيانات
- **تتبع الهيكل**: عرض الموظفين حسب الكيان المعين إليه

## 📱 واجهة المستخدم

### الصفحة الرئيسية (/employees)
```javascript
// عناصر واجهة المستخدم الرئيسية
- كروت الإحصائيات (5 كروت)
- شريط البحث والتصفية  
- زر إضافة موظف جديد
- جدول الموظفين التفاعلي
- أزرار العمل لكل موظف
```

### نموذج إضافة الموظف
```javascript
// أقسام النموذج
1. المعلومات الأساسية (رقم الموظف، الاسم، البريد، الهاتف، الهوية)
2. المعلومات الوظيفية (المنصب، القسم، تاريخ التوظيف، الراتب، نوع التوظيف)
3. تعيين الكيان (نوع الكيان والكيان المحدد)
4. معلومات إضافية (العنوان، جهة اتصال الطوارئ)
```

## 🔐 نظام الأذونات

### أدوار المصرح لهم
- **HR**: موظفو الموارد البشرية - وصول كامل لإدارة الموظفين
- **ADMIN**: مديرو النظام - وصول كامل لجميع الميزات
- **مستخدمين آخرين**: لا يمكنهم الوصول لإدارة الموظفين

### مستوى الوصول
```javascript
// شروط الظهور في القائمة
show: perms.isHR() || perms.isAdmin()

// التحقق من الأذونات
isHR: () => currentUser.role === ROLES.HR || currentUser.role === ROLES.ADMIN
```

## 🌐 APIs المستخدمة

### نقاط النهاية الرئيسية
```javascript
GET    /api/employees              // جلب جميع الموظفين
POST   /api/employees              // إضافة موظف جديد  
GET    /api/employees/:id          // جلب بيانات موظف محدد
PUT    /api/employees/:id          // تعديل بيانات الموظف
DELETE /api/employees/:id          // حذف الموظف

GET    /api/branches               // جلب الفروع لتعيين الموظفين
GET    /api/incubators            // جلب الحاضنات لتعيين الموظفين
GET    /api/platforms             // جلب المنصات لتعيين الموظفين  
GET    /api/offices               // جلب المكاتب لتعيين الموظفين
```

### مثال على إضافة موظف جديد
```javascript
const employeeData = {
    employee_number: 'EMP-001',
    full_name: 'أحمد محمد السعيد',
    email: 'ahmed@nayosh.com',
    phone: '+966501234567',
    national_id: '1234567890',
    position: 'مطور برمجيات',
    department: 'تقنية المعلومات',
    hire_date: '2024-01-15',
    salary: 8000.00,
    employment_type: 'FULL_TIME',
    assigned_branch_id: 1  // تعيين للفرع رقم 1
};

await fetchAPI('/employees', {
    method: 'POST',
    body: JSON.stringify(employeeData)
});
```

## 📊 المرئيات والإحصائيات

### كروت الإحصائيات
1. **إجمالي الموظفين** - العدد الكلي لجميع الموظفين
2. **موظفو المقر الرئيسي** - الموظفين المعينين للـ HQ
3. **موظفو الفروع** - موظفين الفروع التجارية  
4. **موظفو الحاضنات** - موظفين حاضنات الأعمال
5. **موظفو المنصات والمكاتب** - المجموع المشترك

### ألوان المؤشرات
- **الإجمالي**: أزرق (`bg-blue-50`, `text-blue-600`)
- **المقر الرئيسي**: بنفسجي (`bg-purple-50`, `text-purple-600`)  
- **الفروع**: أخضر (`bg-green-50`, `text-green-600`)
- **الحاضنات**: برتقالي (`bg-orange-50`, `text-orange-600`)
- **المنصات/المكاتب**: رمادي (`bg-gray-50`, `text-gray-600`)

## 🔄 آلية العمل

### تدفق إضافة موظف جديد
1. **فتح النموذج**: الضغط على زر "إضافة موظف جديد"
2. **ملء البيانات الأساسية**: رقم الموظف، الاسم، البريد، إلخ
3. **المعلومات الوظيفية**: المنصب، القسم، نوع التوظيف
4. **اختيار الكيان**: تحديد نوع الكيان ثم الكيان المحدد
5. **المعلومات الإضافية**: العنوان وجهات الاتصال للطوارئ
6. **الإرسال**: التحقق من البيانات وإرسال الطلب
7. **التحديث**: إعادة تحميل صفحة الموظفين لإظهار البيانات الجديدة

### تدفق البحث والتصفية  
1. **البحث بالنص**: البحث في أسماء الموظفين
2. **التصفية بالكيان**: اختيار نوع كيان محدد
3. **التحديث التلقائي**: تحديث الجدول فوراً مع كل تغيير
4. **إعادة تعيين المرشحات**: زر لإزالة جميع المرشحات

## 🎨 التصميم والواجهة

### نمط التصميم
- **Tailwind CSS**: إطار عمل CSS متجاوب
- **Font Awesome**: أيقونات احترافية
- **RTL Support**: دعم كامل للغة العربية
- **Responsive Design**: متجاوب مع جميع الشاشات

### الألوان المستخدمة
- **الأساسي**: `blue-600` للعناصر الرئيسية
- **النجاح**: `green-600` للعمليات الناجحة  
- **التحذير**: `orange-600` للتنبيهات
- **الخطر**: `red-600` لعمليات الحذف
- **المعلومات**: `gray-600` للمعلومات العامة

## 🐛 استكشاف الأخطاء

### مشاكل شائعة وحلولها

#### 1. عدم ظهور قائمة الموظفين
```javascript
// التحقق من الأذونات
if (!perms.isHR() && !perms.isAdmin()) {
    console.log('المستخدم لا يملك صلاحيات الوصول لإدارة الموظفين');
}
```

#### 2. فشل في تحميل الكيانات
```javascript
// التحقق من الـ API
try {
    const entities = await fetchAPI('/branches');
    console.log('تم تحميل الكيانات بنجاح:', entities);
} catch (error) {
    console.error('خطأ في تحميل الكيانات:', error);
}
```

#### 3. خطأ في إضافة موظف
```javascript
// التحقق من البيانات المطلوبة
const requiredFields = ['employee_number', 'full_name', 'position', 'department', 'employment_type'];
const missingFields = requiredFields.filter(field => !employeeData[field]);
if (missingFields.length > 0) {
    console.error('حقول مفقودة:', missingFields);
}
```

## 🔮 التطويرات المستقبلية

### ميزات مخطط إضافتها
1. **تعديل الموظفين**: نموذج تعديل شامل
2. **تقارير الموظفين**: تقارير تفصيلية وإحصائيات
3. **إدارة الحضور**: تتبع حضور وانصراف الموظفين
4. **تقييم الأداء**: نظام تقييم دوري للموظفين
5. **إدارة الرواتب**: حساب وإدارة رواتب الموظفين
6. **الأرشفة**: أرشفة الموظفين المغادرين

### تحسينات تقنية
1. **تحسين الأداء**: pagination للموظفين
2. **تصدير البيانات**: تصدير Excel/PDF
3. **رفع الملفات**: رفع صور وملفات الموظفين
4. **الإشعارات**: تنبيهات لأعياد الميلاد والذكريات
5. **التكامل**: ربط مع أنظمة HR خارجية

---

## 📝 ملاحظات للمطورين

### هيكل الملفات المرتبطة
```
├── server.js              # APIs الخاصة بالموظفين
├── script.js             # واجهة المستخدم والوظائف 
├── index.html            # نموذج إضافة الموظف
├── add-entity-relationships.sql  # Schema قاعدة البيانات
└── EMPLOYEE_MANAGEMENT_GUIDE.md # هذا الملف
```

### المتطلبات التقنية
- Node.js 16+
- PostgreSQL 13+
- المتصفحات الحديثة (Chrome, Firefox, Safari, Edge)

### الاختبار
```bash
# تشغيل الخادم
npm start

# الوصول للنظام  
http://localhost:3000

# اختبار APIs
npm run test:employees
```

---
*تم إنشاء هذا الدليل كجزء من نظام نايوش المتكامل لإدارة موارد المؤسسات*